// 'use strict';
const PageController = function (mainCtrl) {
  const self = this;

  const initPageBtns = (page) => {
    console.log('Initializing page buttons.');
  }

  return {
    initPageBtns(page) {
      initPageBtns(page);
    },
  };
};
